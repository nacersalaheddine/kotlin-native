buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-native-gradle-plugin:+"
    }
}

apply plugin: 'kotlin-native-application'

sourceSets {
    foo {

    }
}

task ttt {
    doLast {
        println(sourceSets)
        println(sourceSets.main)
        println(sourceSets.main.name)
        println(sourceSets.foo.name)
    }
}